### **Running the Project with Streamlit**

Run from the project root (inside the `venv`) using the Streamlit runner so the context and server start properly.

```sh
streamlit run SignLoom.py
```

To pass arguments or select a specific port:

```sh
streamlit run SignLoom.py --server.port 8501 --server.address 0.0.0.0
```
